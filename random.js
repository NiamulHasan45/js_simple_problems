for(let n = 0; n<20; n++){
    const number = Math.random()*6;
    const rNumber = Math.round(number);
    console.log(rNumber);
}

const fNumber = 12.456;
console.log(Math.ceil(fNumber));
console.log(Math.floor(fNumber));
console.log(Math.round(fNumber));